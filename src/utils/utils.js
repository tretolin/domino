export function Subtitle(props) {
    return <h2 className="size-30">~ { props.title } ~</h2>
}

export function Button(props) {
    return <div className="domino-button text-center text-uppercase">{ props.title }</div>
}